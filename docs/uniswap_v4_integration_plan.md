# План интеграции коннектора Uniswap V4 в основной поток данных

## 1. Анализ и фиксация исходного состояния
- **1.1.1.** Считать текущий скрипт `test_scripts/uniswap/v4/connect_uniswap_v4.go` и зафиксировать, какие типы событий (Swap, Mint/Burn, другие логи) он декодирует и какие поля извлекает.
- **1.1.2.** Составить таблицу используемых структур (`v4PoolMeta`, `v4PoolEntry`, `v4LogItem`) и отметить, какие из них нужно перенести в производственный пакет.
- **1.1.3.** Выписать зависимости скрипта (ABI-файлы, JSON со списком пулов, переменные окружения) и проверить их наличие в репозитории.
- **1.2.1.** Проанализировать производственный поток данных (`internal/dex/Etherium/Uniswap/v2_connector.go`, `connect_uniswap_v3_all.go`, `cmd/screener-core/main.go`) и описать, как коннекторы передают сообщения в общий `DataChannel` и как эти сообщения попадают в Redis.
- **1.2.2.** Уточнить требования к расчёту цены: актуальный алгоритм реализован в `internal/dex/pricing/graph_pricer.go`, необходимо подтвердить, какие вызовы `UpdatePair` и `ResolveUSD` обязательны для соответствия V4.
- **1.2.3.** Проверить, какие конфигурационные параметры нужны для запуска V2/V3, и определить, какие аналоги потребуются для V4 (WS URL, HTTP URL для `eth_call`, пути к пулам, батчи, флаги логирования).

## 2. Проектирование целевой архитектуры коннектора V4
- **2.1.1.** Определить, будет ли V4 использовать отдельную структуру `V4Config` или расширит существующие конфиги; зафиксировать поля и их типы.
- **2.1.2.** Разработать схему потоков данных: подписка по WebSocket → обработка событий → обновление `GraphPricer` → формирование `pb.MarketData` → отправка в `DataChannel` → Redis.
- **2.1.3.** Прописать стратегию управления подключением (handshake, ping/pong, reconnect backoff) с учётом ограничений Alchemy и опыта V3-коннектора.
- **2.1.4.** Спроектировать подсистему ленивой загрузки метаданных (decimals, symbol) через `eth_call`, чтобы избежать блокировок канала.
- **2.1.5.** Описать механизм семафора/пула воркеров для параллельных запросов метаданных, опираясь на реализацию `v3MetaSem`.
- **2.1.6.** Решить, как обрабатывать появление новых пулов (динамическая подписка vs. статический список) и зафиксировать подход в документации.

## 3. Подготовка конфигурации и данных пулов
- **3.1.1.** Добавить в `configs/screener-core.yaml` пример секции `dex_configs` для `uniswap_v4` с обязательными полями и комментариями.
- **3.1.2.** Расширить `internal/config/config.go`, чтобы `DexConfig` понимал новые поля (например, `max_meta_workers`, `swap_only`, `stop_on_ack_error`, если они актуальны для V4).
- **3.1.3.** Обновить `internal/config` для валидации значений V4 (непустой `ws_url`, корректные пути к пулам, диапазоны для батчей и таймаутов).
- **3.1.4.** Если V4 использует GeckoTerminal JSON, расширить `ticker_source/geckoterminal_pools.json` или добавить отдельный файл с пулов и описать формат (PoolManager address, tokens, decimals).
- **3.1.5.** Добавить в `docs/project_documentation.md` раздел о настройке V4 (URL, источники пулов, ограничения подписки).
- **3.1.6.** Подготовить fallback-набор пулов для smoke-тестов (минимум 1-2 пула типа `UNI/USDC`, `LINK/USDC`).

## 4. Реализация производственного пакета Uniswap V4
- **4.1.1.** Создать файл `internal/dex/Etherium/Uniswap/v4_connector.go` с публичным конструктором `NewV4Connector(V4Config, pricing.Pricer)`.
- **4.1.2.** Перенести и адаптировать структуры данных из тестового скрипта в пакет (`PoolMeta`, `GeckoEntry`, `RPCRequest`, `SubNote`) с единым префиксом без `v4` в именах.
- **4.1.3.** Реализовать метод `Run(ctx context.Context, out chan<- *pb.MarketData)` с циклом подписки, аналогичным V3, но учитывающим специфику V4 событий (PoolManager).
- **4.1.4.** Встроить разбор ABI из `ABI/Uniswap/V4/UniswapV4PoolManager.json`; предусмотреть кэш и проверку на повторную загрузку.
- **4.1.5.** Добавить загрузку пулов из файла/конфига с валидацией адресов (`common.IsHexAddress`) и decimals.
- **4.1.6.** Реализовать конвертацию `sqrtPriceX96` (или соответствующего поля V4) в цену с учётом decimals токенов.
- **4.1.7.** Интегрировать расчёт веса сделки (объёмов) по аналогии с `v3CalcWeight`, адаптировав под события V4.
- **4.1.8.** Добавить обработку ошибок подписки/ack (логирование, retry, опциональное завершение при `stop_on_ack_error`).
- **4.1.9.** Поддержать опцию `once` (как в тестовом скрипте) только через конфиг/флаги, если она нужна для дебага, иначе задокументировать отсутствие.
- **4.1.10.** Обеспечить конкурентно-безопасное обновление мап с метаданными (использовать `sync.RWMutex` или `sync.Map`).

## 5. Интеграция с графовым прайс-агрегатором
- **5.1.1.** После загрузки пула регистрировать оба токена в `GraphPricer` (вызовы `RegisterToken`/`RegisterStable`).
- **5.1.2.** В обработчике каждого события формировать `price1Per0` и `price0Per1` в виде `*big.Rat` и конвертировать в `float64`.
- **5.1.3.** Передавать пары в `UpdatePair` с вычисленным весом и `time.Now()`.
- **5.1.4.** Для каждого обновлённого токена вызывать `ResolveUSD`; при успехе публиковать `MarketData` с суффиксом `USD` в общий канал.
- **5.1.5.** Гарантировать, что прайсер создаётся в `cmd/screener-core` даже при отсутствии других DEX, если V4 включён.
- **5.1.6.** Добавить метрики/логи количества успешных обновлений и проблем с конверсией цены (NaN/Inf).

## 6. Подключение к общему лаунчеру и каналу данных
- **6.1.1.** Зарегистрировать новый билдер в `internal/launcher/register_dex.go` (`Register("uniswap_v4", buildUniswapV4)`).
- **6.1.2.** Реализовать `buildUniswapV4`, который создаёт конфиг, коннектор и запускает `Supervisor` с привязкой к `ctx.Stop`.
- **6.1.3.** Убедиться, что `buildUniswapV4` передаёт `ctx.Pricer` и `ctx.DataChannel` в коннектор.
- **6.1.4.** Добавить поддержку `uniswap_v4` в валидацию конфигов (ошибка, если коннектор включён без прайсера или без assets registry).
- **6.1.5.** Проверить, что при выключении сервиса (закрытие `stop`) V4 корректно завершает горутины и закрывает WS.

## 7. Интеграция с Redis пайплайном и нормализацией символов
- **7.1.1.** Убедиться, что создаваемые `MarketData` содержат канонический `Exchange` (`uniswap_v4` или кастомное имя из конфига).
- **7.1.2.** Использовать `util.NormalizeSpotSymbol` для тикеров перед публикацией, чтобы не нарушать ключи `price_canon:*`.
- **7.1.3.** Проверить, что worker-пулы Redis (`cmd/screener-core/main.go`) не требуют доработок, и документировать ожидаемый throughput V4.
- **7.1.4.** При необходимости добавить отдельный канал метрик по количеству сообщений V4 (по аналогии с `perExchange`).

## 8. Тестирование и проверка корректности
- **8.1.1.** Написать unit-тесты на парсинг JSON пулов и формулы цены (sqrtPrice → float) в `internal/dex/Etherium/Uniswap/v4_connector_test.go`.
- **8.1.2.** Добавить интеграционный тест, который поднимает mock WebSocket и проверяет цепочку: событие → `UpdatePair` → публикация `MarketData` → запись в тестовый Redis (можно использовать `redisclient` mock).
- **8.1.3.** Подготовить сценарий ручного прогона (Go binary) для smoke-теста на реальной сети с ограниченным списком пулов.
- **8.1.4.** Включить тесты V4 в CI (go test ./...) и обновить документацию по запуску.
- **8.1.5.** Проверить, что `go vet`, `staticcheck` и линтеры проходят после добавления кода.

## 9. Документация и операционное сопровождение
- **9.1.1.** Обновить `docs/README.md` и `docs/project_documentation.md` разделом «Uniswap V4», описав параметры, ограничения и шаги запуска.
- **9.1.2.** Добавить подсказки в `plan.md`/`docs` о миграции с V3 на V4, если требуется.
- **9.1.3.** Описать процедуру мониторинга (какие логи сигнализируют об ошибках ack, как проверять задержку в Redis).
- **9.1.4.** Обновить `scripts/start_all.sh`/`stop_all.sh`, если они должны управлять новым бинарем или конфигом.
- **9.1.5.** Подготовить чеклист релиза: обновление конфигурации, перезапуск сервисов, проверка метрик.

## 10. Дальнейшее развитие и обратная связь
- **10.1.1.** Собрать требования к поддержке V4-пулов с нестандартными токенами (LP-токены, стейблы без decimals) и учесть их в бэклоге.
- **10.1.2.** Планировать итерацию по оптимизации производительности (batch-подписки, компрессия сообщений) после базового запуска.
- **10.1.3.** Настроить обратную связь от трейдинговой команды: формат отчетов по качеству данных, SLA на обновление цен.
