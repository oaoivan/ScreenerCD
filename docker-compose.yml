version: '3.8'

services:
  screener-core:
    build:
      context: .
      dockerfile: Dockerfile.screener-core
    depends_on:
      - redis

  redis:
    image: "redis:7.2-alpine"
    ports:
      - "6380:6379"